[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "abstraction-watch"
version = "0.1.0"
description = "The present, and whether it has stopped changing. Change and quiet as one subscription."
license = "Apache-2.0"
requires-python = ">=3.9"
dependencies = []

[project.urls]
Source = "https://github.com/openabstractions/abstraction-watch"

[tool.setuptools]
py-modules = ["abstraction_watch"]
